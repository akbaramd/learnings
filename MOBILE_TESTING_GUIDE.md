# راهنمای تست روی موبایل 📱

این راهنما به شما کمک می‌کند تا اپلیکیشن Next.js را مستقیماً روی موبایل خود تست کنید.

## روش 1: استفاده از IP محلی (ساده‌ترین روش) 🌐

### مرحله 1: پیدا کردن IP آدرس کامپیوتر

**ویندوز:**
```bash
ipconfig
```
به دنبال `IPv4 Address` در بخش `Wireless LAN adapter Wi-Fi` یا `Ethernet adapter` بگردید.
مثال: `192.168.1.100`

**مک/لینوکس:**
```bash
ifconfig
# یا
ip addr
```
به دنبال IP در بخش `inet` بگردید.

### مرحله 2: اجرای سرور با دسترسی شبکه

سرور Next.js را با IP آدرس 0.0.0.0 اجرا کنید تا از شبکه محلی قابل دسترسی باشد:

```bash
# در فایل server.js یا package.json تغییر دهید
# یا مستقیماً اجرا کنید:
PORT=3000 HOSTNAME=0.0.0.0 npm run dev
```

**یا در `package.json` اضافه کنید:**
```json
{
  "scripts": {
    "dev:mobile": "HOSTNAME=0.0.0.0 PORT=3000 node server.js"
  }
}
```

**ویندوز (PowerShell):**
```powershell
$env:HOSTNAME="0.0.0.0"; $env:PORT="3000"; npm run dev
```

**ویندوز (CMD):**
```cmd
set HOSTNAME=0.0.0.0 && set PORT=3000 && npm run dev
```

### مرحله 3: اتصال موبایل

1. **موبایل و کامپیوتر باید به یک Wi-Fi متصل باشند**
2. در مرورگر موبایل وارد شوید:
   ```
   http://192.168.1.100:3000
   ```
   (IP آدرس خود را جایگزین کنید)

### مرحله 4: رفع مشکل Firewall (در صورت نیاز)

اگر موبایل نمی‌تواند به سرور متصل شود:

**ویندوز:**
1. Windows Defender Firewall را باز کنید
2. Advanced settings
3. Inbound Rules > New Rule
4. Port > TCP > Specific local ports: 3000
5. Allow the connection
6. تمام Profile ها را انتخاب کنید

**مک:**
```bash
# در System Preferences > Security & Privacy > Firewall
# یا از Terminal:
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/local/bin/node
```

## روش 2: استفاده از ngrok (برای تست از راه دور) 🚀

اگر می‌خواهید از اینترنت (نه فقط شبکه محلی) تست کنید:

### نصب ngrok:
```bash
npm install -g ngrok
# یا
brew install ngrok  # Mac
choco install ngrok  # Windows
```

### اجرا:
```bash
# Terminal 1: سرور Next.js
npm run dev

# Terminal 2: ngrok
ngrok http 3000
```

ngrok یک URL عمومی می‌دهد مثل:
```
https://abc123.ngrok.io
```

این URL را در موبایل باز کنید (حتی از شبکه‌های مختلف).

## روش 3: استفاده از Chrome DevTools (برای شبیه‌سازی) 📲

برای تست سریع بدون نیاز به موبایل واقعی:

1. Chrome را باز کنید
2. F12 (DevTools)
3. Toggle Device Toolbar (Ctrl+Shift+M)
4. Device را انتخاب کنید (iPhone, Samsung, etc.)
5. Responsive را برای اندازه دلخواه تنظیم کنید

**محدودیت:** این روش UI را شبیه‌سازی می‌کند اما رفتار واقعی موبایل (مثل viewport height) را کاملاً شبیه‌سازی نمی‌کند.

## روش 4: استفاده از USB Debugging (اندروید) 🔌

برای تست دقیق‌تر روی دستگاه واقعی:

### تنظیمات:

1. **روی موبایل:**
   - Settings > About Phone
   - 7 بار روی "Build Number" بزنید (Developer Options فعال می‌شود)
   - Settings > Developer Options > USB Debugging را فعال کنید

2. **روی کامپیوتر:**
   - Chrome را باز کنید
   - `chrome://inspect` را وارد کنید
   - موبایل را با USB وصل کنید
   - "Port forwarding" را فعال کنید
   - Port: 3000

3. **در موبایل:**
   - Chrome را باز کنید
   - `localhost:3000` را وارد کنید

## روش 5: استفاده از Expo Go یا React Native Debugger (برای PWA) 📱

اگر اپلیکیشن PWA است:

1. سرور را با IP محلی اجرا کنید (روش 1)
2. در موبایل Chrome/Safari باز کنید
3. URL را وارد کنید
4. "Add to Home Screen" را بزنید
5. به عنوان PWA تست کنید

## نکات مهم ⚠️

### 1. امنیت
- **هرگز سرور dev را در اینترنت عمومی قرار ندهید**
- فقط در شبکه محلی یا با ngrok (که موقت است) استفاده کنید
- برای production از HTTPS استفاده کنید

### 2. Performance
- سرور dev کندتر از production است
- برای تست performance واقعی، build کنید:
  ```bash
  npm run build
  npm start
  ```

### 3. Hot Reload
- تغییرات کد به صورت خودکار در موبایل اعمال می‌شود
- اگر کار نکرد، صفحه را refresh کنید

### 4. Network Issues
- مطمئن شوید موبایل و کامپیوتر در یک شبکه هستند
- Firewall را چک کنید
- IP آدرس را دوباره بررسی کنید (ممکن است تغییر کند)

## تست مشکلات Viewport Height 📐

برای تست مشکل ارتفاع viewport:

1. صفحه را refresh کنید (F5 یا pull to refresh)
2. چک کنید که bottom navigation زیر اندیکاتورهای موبایل نرود
3. صفحه را scroll کنید
4. مرورگر را minimize/maximize کنید
5. دستگاه را rotate کنید (portrait/landscape)

## دستورات سریع 🚀

```bash
# پیدا کردن IP (ویندوز)
ipconfig | findstr IPv4

# پیدا کردن IP (مک/لینوکس)
ifconfig | grep "inet " | grep -v 127.0.0.1

# اجرای سرور با دسترسی شبکه (ویندوز PowerShell)
$env:HOSTNAME="0.0.0.0"; npm run dev

# اجرای سرور با دسترسی شبکه (مک/لینوکس)
HOSTNAME=0.0.0.0 npm run dev

# Build و اجرای production
npm run build && npm start
```

## عیب‌یابی 🔧

### مشکل: موبایل نمی‌تواند به سرور متصل شود
- ✅ IP آدرس را دوباره چک کنید
- ✅ Firewall را غیرفعال کنید یا port 3000 را allow کنید
- ✅ مطمئن شوید موبایل و کامپیوتر در یک Wi-Fi هستند
- ✅ سرور را با `HOSTNAME=0.0.0.0` اجرا کنید

### مشکل: صفحه load نمی‌شود
- ✅ سرور در حال اجرا است؟
- ✅ Port 3000 درست است؟
- ✅ URL را با `http://` شروع کنید (نه `https://`)

### مشکل: تغییرات اعمال نمی‌شود
- ✅ صفحه را hard refresh کنید (Ctrl+Shift+R)
- ✅ Cache را پاک کنید
- ✅ سرور را restart کنید

## خلاصه روش‌ها 📋

| روش | سادگی | دقت | نیاز به تنظیم |
|-----|-------|-----|---------------|
| IP محلی | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | کم |
| ngrok | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | متوسط |
| Chrome DevTools | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | هیچ |
| USB Debugging | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | زیاد |

**توصیه:** برای تست سریع از **روش 1 (IP محلی)** استفاده کنید.

