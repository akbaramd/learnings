# 🔐 Authentication Flow - Visual Diagrams

## 📊 Login Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER ACTION                               │
│              Visits /login or clicks "ورود"                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MIDDLEWARE CHECK                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ if (has cookies && !logout=true)                          │  │
│  │   → Redirect to /dashboard                                 │  │
│  │ else                                                       │  │
│  │   → Allow access to /login                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    LOGIN PAGE (/login)                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ useAuth() hook checks:                                    │  │
│  │   - isAuthenticated? → Redirect to dashboard              │  │
│  │   - isLogoutFlow? → Set status to 'anonymous'            │  │
│  │                                                           │  │
│  │ User enters: National Code (کد ملی)                        │  │
│  │ User clicks: "ارسال کد"                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              SEND OTP MUTATION                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Client: POST /api/auth/login                              │  │
│  │ Body: { nationalCode: "1234567890" }                      │  │
│  │                                                           │  │
│  │ BFF Route: app/api/auth/login/route.ts                   │  │
│  │   → Calls: POST /auth/login (upstream)                    │  │
│  │   → Returns: { challengeId, maskedPhoneNumber }          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              REDUX STATE UPDATE                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ dispatch(setChallengeId(challengeId))                    │  │
│  │ dispatch(setMaskedPhoneNumber(maskedPhone))                │  │
│  │ dispatch(setNationalCode(nationalCode))                  │  │
│  │ dispatch(setAuthStatus('otp-sent'))                      │  │
│  │                                                           │  │
│  │ State:                                                    │  │
│  │   status: 'otp-sent'                                      │  │
│  │   challengeId: "abc123..."                                │  │
│  │   maskedPhoneNumber: "0912***1234"                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              AUTO REDIRECT                                       │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ useEffect detects:                                         │  │
│  │   challengeId && authStatus === 'otp-sent'                │  │
│  │                                                           │  │
│  │ Redirects to:                                             │  │
│  │   /verify-otp?r={returnUrl}&logout={flag}                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              VERIFY OTP PAGE (/verify-otp)                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Shows: "کد به شماره 0912***1234 ارسال شد"                  │  │
│  │                                                           │  │
│  │ User enters: 6-digit OTP code                            │  │
│  │ User clicks: "تأیید"                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              VERIFY OTP MUTATION                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Client: POST /api/auth/verify-otp                         │  │
│  │ Body: { challengeId, otpCode }                           │  │
│  │                                                           │  │
│  │ BFF Route: app/api/auth/verify-otp/route.ts               │  │
│  │   → Calls: POST /auth/verify-otp (upstream)               │  │
│  │   → Server sets cookies:                                   │  │
│  │     * accessToken (httpOnly, secure)                      │  │
│  │     * refreshToken (httpOnly, secure)                    │  │
│  │   → Returns: { userId, isSuccess: true }                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              REDUX STATE UPDATE                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ dispatch(clearChallengeId())                              │  │
│  │ dispatch(setAuthStatus('authenticated'))                  │  │
│  │ dispatch(authApi.endpoints.getMe.initiate())             │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              FETCH USER PROFILE                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Client: GET /api/auth/me                                  │  │
│  │                                                           │  │
│  │ BFF Route: app/api/auth/me/route.ts                       │  │
│  │   → Calls: GET /auth/me (upstream)                        │  │
│  │   → Returns: { id, name, roles, ... }                    │  │
│  │                                                           │  │
│  │ Redux Update:                                             │  │
│  │   dispatch(setUser(userProfile))                          │  │
│  │   dispatch(setInitialized(true))                          │  │
│  │   dispatch(setAuthStatus('authenticated'))                │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              REDIRECT TO DESTINATION                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ safeResolveReturnUrl() logic:                             │  │
│  │   if (logout=true) → /dashboard                            │  │
│  │   else if (returnUrl exists) → returnUrl                   │  │
│  │   else → /dashboard                                        │  │
│  │                                                           │  │
│  │ router.replace(redirectTo)                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ✅ USER LOGGED IN                            │
│              Status: 'authenticated'                             │
│              Cookies: accessToken, refreshToken                 │
│              Redux: user data loaded                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚪 Logout Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER ACTION                               │
│              Clicks "خروج از حساب" on logout page                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              CLEAR LOCAL STATE FIRST                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ dispatch(clearUser())                                    │  │
│  │ dispatch(clearChallengeId())                              │  │
│  │ dispatch(setAnonymous())                                  │  │
│  │                                                           │  │
│  │ State immediately changes:                                 │  │
│  │   status: 'anonymous'                                     │  │
│  │   user: null                                              │  │
│  │   challengeId: null                                      │  │
│  │                                                           │  │
│  │ ⚠️ This triggers ProtectedLayout to detect change         │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              CALL LOGOUT API (if authenticated)                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Client: POST /api/auth/logout                              │  │
│  │ Body: { refreshToken: undefined }                          │  │
│  │                                                           │  │
│  │ BFF Route: app/api/auth/logout/route.ts                    │  │
│  │   → Gets tokens from cookies                               │  │
│  │   → If no tokens: Return success (already logged out)     │  │
│  │   → Else: Calls POST /auth/logout (upstream)              │  │
│  │   → Clears cookies: maxAge: 0                             │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              REDUX STATE UPDATE                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ dispatch(clearUser())                                    │  │
│  │ dispatch(clearChallengeId())                              │  │
│  │ dispatch(setAuthStatus('anonymous'))                      │  │
│  │                                                           │  │
│  │ State:                                                    │  │
│  │   status: 'anonymous'                                     │  │
│  │   user: null                                              │  │
│  │   isInitialized: true (from baseApi.ts if 401)            │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              PROTECTED LAYOUT DETECTS CHANGE                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ useEffect([isAuthenticated, isReady, authStatus])        │  │
│  │                                                           │  │
│  │ Priority Check:                                           │  │
│  │   1. if (authStatus === 'anonymous')                      │  │
│  │      → Redirect immediately (even if !isReady)           │  │
│  │   2. if (!isReady) → Wait                                │  │
│  │   3. if (!isAuthenticated) → Redirect                    │  │
│  │                                                           │  │
│  │ window.location.href =                                    │  │
│  │   '/login?logout=true&r={currentPath}'                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              MIDDLEWARE CHECK                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Sees: /login?logout=true                                 │  │
│  │                                                           │  │
│  │ Logic:                                                    │  │
│  │   if (logout=true)                                        │  │
│  │     → Allow access (even if cookies exist)              │  │
│  │   else if (has cookies)                                  │  │
│  │     → Redirect to /dashboard                             │  │
│  │                                                           │  │
│  │ ⚠️ This prevents redirect loop                          │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              LOGIN PAGE                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Detects: logout=true                                      │  │
│  │                                                           │  │
│  │ Actions:                                                  │  │
│  │   - Sets status to 'anonymous' if needed                 │  │
│  │   - Allows user to login again                           │  │
│  │   - After login, redirects to /dashboard (not returnUrl) │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ✅ USER LOGGED OUT                           │
│              Status: 'anonymous'                                │
│              Cookies: Cleared (maxAge: 0)                        │
│              Redux: All auth data cleared                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Token Refresh Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│              CLIENT MAKES API REQUEST                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ GET /api/wallets/balance                                   │  │
│  │ Cookies: accessToken (expired), refreshToken (valid)        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              BFF ROUTE                                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ app/api/wallets/balance/route.ts                          │  │
│  │   → Uses createApiInstance(req)                          │  │
│  │   → Generated client includes cookies automatically       │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              GENERATED CLIENT (generatedClient.ts)               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Axios Request Interceptor:                                │  │
│  │   → Sends request to upstream                             │  │
│  │   → Upstream returns: 401 Unauthorized                    │  │
│  │                                                           │  │
│  │ Axios Response Interceptor:                                │  │
│  │   → Detects 401                                           │  │
│  │   → Automatically calls: POST /auth/refresh               │  │
│  │   → If refresh succeeds:                                  │  │
│  │     * New tokens set in cookies                            │  │
│  │     * Retries original request                            │  │
│  │     * Returns 200 OK to client                            │  │
│  │   → If refresh fails:                                     │  │
│  │     * Returns 401 to client                               │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              CLIENT RECEIVES RESPONSE                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Case 1: Refresh Succeeded                                │  │
│  │   → Status: 200 OK                                        │  │
│  │   → Data: Wallet balance                                  │  │
│  │   → Client never knows token was refreshed              │  │
│  │                                                           │  │
│  │ Case 2: Refresh Failed                                    │  │
│  │   → Status: 401 Unauthorized                              │  │
│  │   → Error: "Token refresh failed" or "Session expired"   │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              BASE QUERY HANDLES 401 (baseApi.ts)                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ baseQueryWithReauth detects:                              │  │
│  │   - 401 status code                                       │  │
│  │   - OR "Token refresh failed" message                     │  │
│  │                                                           │  │
│  │ Actions:                                                  │  │
│  │   dispatch(clearUser())                                    │  │
│  │   dispatch(setAnonymous())                                │  │
│  │   dispatch(setInitialized(true))                          │  │
│  │                                                           │  │
│  │ ⚠️ This ensures isReady becomes true                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              PROTECTED LAYOUT DETECTS CHANGE                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Detects: authStatus === 'anonymous'                       │  │
│  │                                                           │  │
│  │ Redirects to:                                             │  │
│  │   /login?logout=true&r={currentPath}                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🛡️ Route Protection Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│              USER NAVIGATES TO ROUTE                            │
│              Example: /dashboard or /surveys/123                 │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              MIDDLEWARE CHECK (middleware.ts)                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ if (pathname.startsWith('/public/'))                     │  │
│  │   → Allow (public route)                                  │  │
│  │                                                           │  │
│  │ if (pathname.startsWith('/api'))                           │  │
│  │   → Allow (API route)                                    │  │
│  │                                                           │  │
│  │ if (isProtectedPath && !hasCookies)                      │  │
│  │   → Redirect to /login?r={pathname}                      │  │
│  │                                                           │  │
│  │ if ((/login || /verify-otp) && hasCookies && !logout)    │  │
│  │   → Redirect to /dashboard or returnUrl                   │  │
│  │                                                           │  │
│  │ if ((/login || /verify-otp) && logout=true)              │  │
│  │   → Allow (logout flow)                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              PROTECTED LAYOUT CHECK                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ useEffect([isAuthenticated, isReady, authStatus])        │  │
│  │                                                           │  │
│  │ Priority 1: Check authStatus                              │  │
│  │   if (authStatus === 'anonymous')                         │  │
│  │     → window.location.href = '/login?logout=true&r=...'  │  │
│  │     → Return (immediate redirect, even if !isReady)      │  │
│  │                                                           │  │
│  │ Priority 2: Wait for ready                                │  │
│  │   if (!isReady)                                           │  │
│  │     → Return (wait for initialization)                    │  │
│  │                                                           │  │
│  │ Priority 3: Check authentication                          │  │
│  │   if (!isAuthenticated)                                  │  │
│  │     → window.location.href = '/login?logout=true&r=...'  │  │
│  │                                                           │  │
│  │ If all checks pass:                                       │  │
│  │   → Render children (allow access)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              RESULT                                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ ✅ Authenticated: Render protected page                   │  │
│  │ ❌ Not Authenticated: Redirect to /login                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 State Machine Diagram

```
                    ┌─────────┐
                    │  IDLE   │ (Initial state)
                    └────┬────┘
                         │
                         │ sendOtp()
                         ▼
                    ┌─────────┐
                    │ LOADING │
                    └────┬────┘
                         │
                         │ OTP sent successfully
                         ▼
                    ┌─────────┐
                    │OTP-SENT │
                    └────┬────┘
                         │
                         │ verifyOtp()
                         ▼
                    ┌─────────┐
                    │LOADING  │
                    └────┬────┘
                         │
                         │ OTP verified successfully
                         │ getMe() completes
                         ▼
              ┌──────────────────────┐
              │  AUTHENTICATED       │
              │  (User logged in)    │
              └──────┬───────────────┘
                     │
                     │ logout() OR 401 error
                     ▼
              ┌──────────────────────┐
              │   ANONYMOUS          │
              │   (User logged out)  │
              └──────┬───────────────┘
                     │
                     │ sendOtp() again
                     └───────────────┐
                                     │
                                     ▼
                              ┌─────────┐
                              │  ERROR  │ (On API failures)
                              └─────────┘
```

---

## 🔑 Key State Transitions

| From State | Action | To State | Trigger |
|------------|--------|----------|---------|
| `idle` | `sendOtp()` | `loading` | User submits national code |
| `loading` | OTP sent | `otp-sent` | Server returns challengeId |
| `otp-sent` | `verifyOtp()` | `loading` | User submits OTP |
| `loading` | OTP verified | `authenticated` | Server sets cookies |
| `authenticated` | `logout()` | `anonymous` | User clicks logout |
| `authenticated` | 401 error | `anonymous` | Token refresh failed |
| `anonymous` | `sendOtp()` | `loading` | User starts login again |
| Any | API error | `error` | API call fails |

---

## 🎯 Critical Points

### **1. isReady Logic**
```
isReady = isInitialized && !isLoading
```

**When isInitialized becomes true:**
- ✅ `getMe` query completes (success or error)
- ✅ `baseQueryWithReauth` detects 401 and sets it explicitly
- ❌ NOT set during initial page load until first query completes

### **2. Status Priority in ProtectedLayout**
```
Priority Order:
1. authStatus === 'anonymous' → Redirect (even if !isReady)
2. !isReady → Wait
3. !isAuthenticated → Redirect
```

**Why this order?**
- Logout can happen before `isReady` becomes `true`
- Checking `authStatus` first handles immediate logout
- Prevents race conditions in production

### **3. Logout Flow Protection**
```
logout=true query param:
- Allows access to /login even if cookies exist
- Prevents middleware redirect loop
- Signals logout flow to login page
```

---

**Last Updated:** Based on current codebase implementation

